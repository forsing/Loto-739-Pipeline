# Core numerics and data handling
numpy>=1.23.0,<2.0.0
pandas>=1.5.0

# Machine learning & clustering
scikit-learn>=1.0.0

# Deep learning
tensorflow>=2.12.0

# Quantum simulation
pennylane>=0.36.0

# Utilities (logging, typing, date handling — usually already in stdlib, but good to have explicit)
python-dateutil>=2.8.0  # used in decay.py for robust parsing